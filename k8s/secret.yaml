apiVersion: v1
kind: Secret
metadata:
  name: valen-secrets
type: Opaque
stringData:
  # IMPORTANT: DO NOT COMMIT REAL SECRETS.
  # Replace this value securely (e.g., using a CI/CD pipeline, SOPS, or SealedSecrets) before applying.
  GEMINI_API_KEY: "replace-with-your-actual-api-key"
